{
  "name": "@marcus-clawdbot/oauthrouter",
  "version": "0.0.1",
  "description": "OAuthRouter scaffold for OpenClaw (re-scaffolded from ClawRouter; payments/provider wiring disabled).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "openclaw.plugin.json",
    "openclaw.security.json"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test:unit": "npm run build --silent && node --test --experimental-test-coverage --test-coverage-lines=45 --test-coverage-functions=40 --test-coverage-branches=30 test/unit/*.test.mjs",
    "test:integration": "npm run build --silent && node --test test/*.test.mjs",
    "test": "npm run test:unit",
    "prepare": "husky"
  },
  "keywords": [
    "oauth",
    "router",
    "llm",
    "openclaw",
    "smart-routing"
  ],
  "author": "marcus-clawdbot",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marcus-clawdbot/oauthrouter.git"
  },
  "peerDependencies": {
    "openclaw": ">=2025.1.0"
  },
  "peerDependenciesMeta": {
    "openclaw": {
      "optional": true
    }
  },
  "dependencies": {
    "proper-lockfile": "^4.1.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/proper-lockfile": "^4.1.4",
    "eslint": "^9.39.2",
    "husky": "^9.1.7",
    "openclaw": "latest",
    "prettier": "^3.8.1",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.54.0"
  },
  "engines": {
    "node": ">=20"
  }
}
