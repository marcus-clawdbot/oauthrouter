{
  "id": "oauthrouter",
  "name": "OAuthRouter",
  "description": "Local OAuth-backed LLM router + localhost proxy",
  "configSchema": {
    "type": "object",
    "properties": {
      "proxyAuthToken": {
        "type": "string",
        "description": "Auth token required on ALL local proxy requests. If omitted, a random token is generated for this process."
      },
      "spendControls": {
        "type": "object",
        "description": "Optional token/quota guardrails (estimated).",
        "properties": {
          "maxRequestInputTokens": {
            "type": "number",
            "description": "Reject requests whose estimated input tokens exceed this value."
          },
          "maxRequestOutputTokens": {
            "type": "number",
            "description": "Reject requests whose requested max_tokens exceed this value."
          },
          "dailyInputTokenBudget": {
            "type": "number",
            "description": "Reject requests once total estimated input tokens for the current UTC day exceeds this value."
          },
          "dailyOutputTokenBudget": {
            "type": "number",
            "description": "Reject requests once total estimated output tokens for the current UTC day exceeds this value."
          },
          "dailyRequestBudget": {
            "type": "number",
            "description": "Reject requests once total requests for the current UTC day exceeds this value."
          },
          "allowlistModels": {
            "type": "array",
            "items": { "type": "string" },
            "description": "If set, only these model IDs are allowed (e.g. 'openai/gpt-4o-mini')."
          },
          "denylistModels": {
            "type": "array",
            "items": { "type": "string" },
            "description": "If set, these model IDs are blocked (e.g. 'anthropic/claude-opus-4')."
          }
        },
        "additionalProperties": false
      },
      "routing": {
        "type": "object",
        "description": "Override default routing configuration"
      }
    },
    "additionalProperties": false
  },
  "uiHints": {
    "proxyAuthToken": {
      "label": "Local Proxy Auth Token",
      "sensitive": true,
      "placeholder": "(optional — auto-generated)"
    }
  }
}
