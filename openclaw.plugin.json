{
  "id": "oauthrouter",
  "name": "OAuthRouter",
  "description": "OAuth-based routing scaffold for OpenClaw",
  "configSchema": {
    "type": "object",
    "properties": {
      "routing": {
        "type": "object",
        "description": "Override default routing configuration (optional)"
      },
      "oauth": {
        "type": "object",
        "description": "OAuth configuration (scaffold; provider wiring not implemented yet)"
      }
    }
  }
}
